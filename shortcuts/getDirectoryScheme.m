% GETDIRECTORYSCHEME Attempt to get the subdirectory scheme for the current
% directory.
%   SCHEME = getDirectoryScheme(DIRECTORY)

function [ sch ] = getDirectoryScheme( fldr )
    subdirs = cd(fldr);
    


end

